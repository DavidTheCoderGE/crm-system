version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    env_file:
      - backend/.env.example
    volumes:
      - ./backend/prisma:/app/prisma
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
